# Environment variables for local development

# Database connection (used by scripts and backend)
# Docker Compose default (port 5433 to avoid conflicts)
DATABASE_URL=postgresql://gisuser:gispass@localhost:5432/spatial_perf

# For production (Oracle Cloud)
# DATABASE_URL=postgresql://gisuser:gispass@db-hostname:5432/spatial_perf

# Backend API
API_HOST=0.0.0.0
API_PORT=8000
